services:
  app:
    build:
      context: .
      target: executor
    hostname: infra
    stdin_open: true
    tty: true
    working_dir: /app
    volumes:
      - "${PWD:-./}:/app"
    environment:
      - HISTSIZE=-1
      - HISTFILESIZE=-1
    network_mode: host
